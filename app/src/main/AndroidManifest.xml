<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lovetto" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity android:name=".activity_tutorial" >
        <intent-filter>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.TAB" />
        </intent-filter>
        </activity>
        <android:theme="@style/AppTheme">
        <activity android:name=".activity_timeline"></activity>
        <activity android:name=".activity_speratura" />
        <activity android:name=".activity_controllo_remoto" />
        <activity android:name=".activity_modifica_dettagli_incubata" />
        <activity android:name=".activity_dettagli_incubatore" />
        <activity android:name=".activity_dettagli_incubata" />
        <activity android:name=".activity_crea_incubata" />
        <activity android:name=".MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
